import { Button, Text, View } from 'react-native';

const TopsMainScreen = () => {
  return (
    <View>
      <Text>This is Props Page text placeholder. Only authorized users allowed here.</Text>
      <Button title="Does nothing"></Button>
    </View>
  );
};

export default TopsMainScreen;
